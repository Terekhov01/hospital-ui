import { AppointmentRegistration } from './appointment-registration';

describe('AppointmentRegistration', () => {
  it('should create an instance', () => {
    expect(new AppointmentRegistration()).toBeTruthy();
  });
});
