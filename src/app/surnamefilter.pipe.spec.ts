import { SurnamefilterPipe } from './surnamefilter.pipe';

describe('SurnamefilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SurnamefilterPipe();
    expect(pipe).toBeTruthy();
  });
});
